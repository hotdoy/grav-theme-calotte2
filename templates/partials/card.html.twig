<article class="card text small">
	<a href="{{ item.url }}">
		<div>{{ item.media.images|first.html()|raw }}</div>
		<h3>{{ item.header.title }}</h3>
		<p>{{ item.summary|striptags }}</p>
	</a>
</article>